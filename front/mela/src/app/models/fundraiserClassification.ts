export interface FundraiserClassification {
  id: number; // Unique identifier for the classification
  name: string; // Name of the classification (e.g., "Celebration," "Startup," "Charity")
  description: string;// A brief description of the classification
  selected: boolean;
}
